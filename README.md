# vokabel.werk

Vokabeltrainer als Web-App/PWA. Next.js 15 (App Router), Tailwind, Supabase (Login + Datenbank).
Eigenständig, unabhängig vom Projekt „werk“.

## Start
```bash
pnpm install
cp .env.example .env.local   # Supabase-URL und Anon-Key eintragen
pnpm dev
```
